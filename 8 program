#include <stdio.h>

int main() {
    int num, lastDigit;

    printf("Enter a number: ");
    scanf("%d", &num);

    lastDigit = num % 10;

    printf("Last digit = %d", lastDigit);

    return 0;
}
